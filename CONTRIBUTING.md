# 협업 규칙 (전원 필독)

> 이 문서를 읽고 나면 어디에 무엇을 만들고, 어떤 이름으로 커밋하고, 누구와 무엇을 맞춰야 하는지 알 수 있다.
> 규칙을 바꿔야 하면 백엔드B에게 알리고 전원에게 공지한다. 혼자 바꾸지 않는다.

## 0. 지금 단계

**본선 당일이고 구현 단계다. 코드를 올린다.** 브랜치와 커밋 규칙만 지키면 된다.

| 올린다 | 올리지 않는다 |
| --- | --- |
| 소스 파일, 테스트 | 가상환경, 의존성 폴더, 빌드 산출물 |
| 의존성 목록 (버전 고정) | 환경 변수 파일, API 키 |
| 정책 데이터 파일 | 대표 프로필 정답셋 (`data/eval/`) |
| 기획·명세 문서, 화면 문구 목록 | 설문 원본과 그 링크 |
| 와이어프레임 이미지 | 실제 사용자 입력 기록 |

문서는 계속 기준으로 남는다. **문서와 코드가 다르면 문서를 먼저 고친다.**
문서에 없는 값이 필요하면 만들지 말고 물어본다 (`.kiro/steering/01-working-agreements.md` 1장).

## 1. 시작하기

1. 저장소를 클론한다.
2. `dev` 브랜치로 이동해 최신 상태를 받는다.
3. `dev`에서 자기 역할 브랜치를 딴다. (브랜치 이름은 2장 표 참고)

읽는 순서:

1. `README.md` — 무엇을 만드는지, P0 세 가지
2. `docs/00-overview.md` — 제품 개요, 왜 이렇게 만드는지
3. `docs/01-glossary-profile.md` — 용어와 프로필 허용 값 (**모든 역할이 같은 값을 써야 한다**)
4. `docs/02-requirements-ears.md` — 기능 요구사항 FR01~FR15
5. `docs/03-api-contract.md` — API와 스트리밍 계약 (프론트·백엔드B 필수)
6. `docs/04-data-schema.md` — 정책 데이터 필드와 판정 결과 항목
7. `docs/05-interfaces.md` — 역할 간에 주고받는 것과 확정 시각
8. 자기 역할 폴더의 `README.md`

본선 당일 09:00 KIRO Spec 요구사항 입력
= `docs/02-requirements-ears.md` + `docs/01-glossary-profile.md` + 자기 역할 폴더 `README.md`.

## 2. 브랜치

| 브랜치 | 용도 | 누가 |
| --- | --- | --- |
| `main` | 발표용. 동결된 상태만 | 백엔드B만 머지 |
| `dev` | 통합 대상. 여기로 모은다 | 전원 PR |
| `feat/frontend` | 화면 | 프론트 |
| `feat/data` | 정책 데이터 | 백엔드A |
| `feat/rules` | 규칙 엔진 | 백엔드A |
| `feat/server` | API·세션·스트리밍 | 백엔드B |
| `feat/ai-conversation` | 메시지 해석·후속 질문·답변 | AI A |
| `feat/ai-judgment` | 예외 조건 판정·인용 검증 | AI B |

- 항상 `dev`에서 따고, `dev`로 PR한다. `main`으로 직접 PR하지 않는다.
- 브랜치 이름은 `feat/`, `fix/`, `docs/`, `chore/` 중 하나로 시작한다.
- 남의 브랜치에 직접 커밋하지 않는다.

## 3. 커밋 메시지

형식: `타입(범위): 한 줄 요약`

| 타입 | 쓸 때 |
| --- | --- |
| `feat` | 기능 추가 |
| `fix` | 버그 수정 |
| `docs` | 문서만 변경 |
| `refactor` | 동작 그대로 구조 변경 |
| `chore` | 설정, 의존성, 스크립트 |
| `data` | 정책 데이터 추가·수정 |

**범위**는 폴더 이름을 쓴다: `frontend`, `rules`, `data`, `server`, `ai-conversation`, `ai-judgment`, `docs`

요약은 한국어 현재형, 마침표 없이, 50자 이내.

| 예시 |
| --- |
| `docs(docs): 프로필 추가 항목에 가구원 수 추가` |
| `data(data): 주거 분야 정책 5건 추가 (SEOUL-011~015)` |
| `feat(rules): 소득 구간과 소득 상한 비교 판정 추가` |
| `feat(frontend): 각주 팝오버에 발췌·출처·확인일 표시` |
| `fix(server): 예외 조건 판정 시간 초과 시 미확인 처리 누락 수정` |

- 커밋은 작게, 하나의 관심사만. 여러 역할 폴더를 한 커밋에 섞지 않는다.
- 전체 추가(`.` 또는 `-A`) 대신 파일을 지정해서 스테이징한다.

## 4. 절대 커밋하지 않는 것

| 대상 | 이유 | 어디에 두는가 |
| --- | --- | --- |
| 대표 프로필 정답셋 | AI A·B에게 공개되면 지표가 의미 없어진다 | 백엔드A 로컬 또는 비공개 시트 |
| 설문 원본 시트와 링크 | 응답자 보호. 공개 문서에는 비식별 요약만 | 공개 범위 제한된 팀 내부 공유 |
| LLM API 키, 계정 정보 | 유출 위험 | 각자 로컬 환경 변수. 채팅·커밋으로 공유 금지 |
| 실제 사용자 입력 기록 | 개인정보 최소화 원칙 | 애초에 수집하지 않는다 |

본선 당일 코드를 올리기 시작할 때 백엔드B가 무시 목록(의존성 폴더, 빌드 산출물, 환경 변수, 정답셋, 설문 원본)을
저장소에 추가한다. 그 전까지는 이 표를 눈으로 확인하고 지킨다.

## 5. PR과 통합

| 항목 | 규칙 |
| --- | --- |
| 통합 시각 | **12:00, 14:00** 두 번. 이 시각 전까지 PR을 올린다 |
| 머지 담당 | 백엔드B만 `dev` → `main` |
| PR 제목 | 커밋 메시지와 같은 형식 |
| PR 본문 | 무엇을 했는지 3줄, 다른 역할에 영향 있으면 명시, 확인한 화면·응답 캡처 |
| 리뷰 | `docs/05-interfaces.md`를 건드리는 PR만 백엔드B 확인 필수. 나머지는 셀프 머지 가능 |
| 충돌 | 자기 역할 폴더 밖 파일에서 충돌이 나면 직접 해결하지 말고 백엔드B에게 |
| 기능 동결 | **14:30**. 이후 `main`에는 버그 수정만 |

## 6. 폴더 소유권

| 경로 | 소유 | 다른 사람이 |
| --- | --- | --- |
| `docs/` | 백엔드B | PR로만 수정 제안 |
| `frontend/` | 프론트 | 건드리지 않음 (백엔드B는 연결 작업 지원 시 가능) |
| `data/` | 백엔드A | 정책 수집분은 각자 추가, 형식 검토는 백엔드A |
| `rules/` | 백엔드A | 건드리지 않음 |
| `server/` | 백엔드B | 건드리지 않음 |
| `ai/conversation/` | AI A | 건드리지 않음 |
| `ai/judgment/` | AI B | 건드리지 않음 |

자기 폴더 밖을 수정해야 하면 소유자에게 말하고 PR로 올린다.

## 7. 이름과 값 컨벤션

**모든 역할이 같은 이름과 값을 써야 한다.** 여기서 어긋나면 통합 때 다 깨진다.

### 7-1. 필드 이름

- 서버가 주고받는 본문과 정책 데이터 필드는 **snake_case**. 예: `policy_id`, `income_max_pct`, `checked_at`, `apply_end`
- 스트리밍 이벤트 이름도 snake_case. 예: `profile_update`, `answer_delta`
- 서버·규칙 엔진·AI 모듈은 **파이썬**이고 파이썬 관습도 snake_case라 이 셋 사이에는 변환이 없다. 문서에 적힌 이름을 그대로 쓴다.
- 변환이 필요한 곳은 **프론트 경계 한 곳뿐**이다. 프론트 내부에서 camelCase를 쓰더라도 서버로 보낼 때와 받을 때 snake_case로 맞춘다.
- 같은 개념에 두 이름을 만들지 않는다. 문서에 있는 이름이 유일한 이름이다.

### 7-1-1. 파이썬 쪽 규칙 (서버·규칙 엔진·AI 모듈)

| 항목 | 규칙 |
| --- | --- |
| 모듈·파일 이름 | 소문자와 밑줄. 예: `deadline.py`, `citation_check.py` |
| 폴더 이름 | 소문자와 하이픈 없이. 파이썬 패키지로 잡히는 폴더는 밑줄 |
| 고정 값 | 문자열을 그대로 쓰지 말고 한 곳에 상수로 모은다. 오타가 판정을 조용히 틀리게 만든다 |
| 날짜 | 문자열 `YYYY-MM-DD`로 주고받고, 계산할 때만 날짜 타입으로 바꾼다. 시간대는 한국 시간 고정 |
| 모델 호출 | 한 모듈에서만 한다. 모델 이름을 여러 파일에 흩지 않는다 |
| 의존성 | 최소로. 버전을 정확히 고정한다. 낯선 패키지 이름은 실제 존재를 확인한다 |
| 가상환경 | 각자 로컬에. 저장소에 올리지 않는다 |

### 7-2. 고정 값 (문자 그대로 쓴다)

| 종류 | 값 |
| --- | --- |
| 판정 상태 | `likely`, `check`, `unlikely` |
| 조건 결과 | `met`(충족), `unmet`(미충족), `unknown`(미확인) |
| 판정 주체 | `rule`, `ai` |
| 데이터 상태 | `verified`(검수 완료), `recheck`(재확인 필요), `closed`(마감), `upcoming`(접수 예정) |
| 의도 | `find_policy`, `policy_question`, `compare`, `result_only`, `out_of_scope`, `smalltalk` |
| 진행 단계 | `searching`, `checking`, `summarizing` |

프로필 항목의 허용 값은 `docs/01-glossary-profile.md` 2~3장이 기준이다.

화면에 보이는 한국어 문구는 값과 분리한다. 문구는 `docs/01-glossary-profile.md`와 각 역할 폴더의 문구 표가 기준.

### 7-3. 날짜와 시간

- 모든 날짜는 `YYYY-MM-DD` 형식. 시간은 넣지 않는다.
- 모든 계산은 **한국 시간(KST, UTC+9)** 기준.
- D-day는 **코드만** 계산한다. AI에게 날짜 계산을 시키지 않는다.

### 7-4. 정책 번호

`지역-세 자리 일련번호`. 서울시는 `SEOUL`, 중앙정부는 `GOV`. 예: `SEOUL-001`, `GOV-007`

### 7-5. 파일과 폴더 이름

- 영어 소문자와 하이픈만. 한글 파일명을 쓰지 않는다.
- 문서는 `번호-주제.md` 형식으로 `docs/`에 둔다.
- 와이어프레임 이미지는 `frontend/wireframes/` 아래에 화면 이름으로 둔다.

## 8. 문서 작성 규칙

- 사용자에게 보이는 문구를 정할 때는 반드시 문구 표에 적는다. 나중에 코드에 직접 쓰지 않는다.
- 결정이 바뀌면 문서를 먼저 고치고 구현한다. **문서와 코드가 다르면 문서가 기준.**
- 확정 안 된 건 "미확정"이라고 적어 둔다. 추측으로 채우지 않는다.
- 표를 먼저 쓰고 설명을 뒤에 둔다. 다른 사람이 표만 보고도 작업할 수 있게.

## 9. 전원 금지 사항

- 공고에 없는 금액·날짜·조건을 화면에 보여주지 않는다. 금액과 날짜는 데이터 필드에서만 가져온다.
- 확정 표현 금지: 대상입니다, 받을 수 있어요, 확실히, 무조건, 100%, 보장, 걱정 마세요
- 이름, 연락처, 주민등록번호, 계좌번호를 받거나 저장하지 않는다.
- 각주 없는 판정 문장을 출력하지 않는다.
- 정답셋을 AI A·B에게 공개하지 않는다.

## 10. 막혔을 때

| 상황 | 누구에게 |
| --- | --- |
| 정책 데이터 해석, 규칙 판정 기준 | 백엔드A |
| API 모양, 이벤트 순서, 배포 | 백엔드B |
| 답변 문구, 후속 질문 | AI A |
| 예외 조건 판정, 인용 검증 | AI B |
| 화면 문구, 레이아웃 | 프론트 |

12:00, 14:00 통합 때 진행 상황을 점검한다.
늦으면 `docs/00-overview.md`의 "빼는 순서"대로 즉시 범위를 줄인다.
